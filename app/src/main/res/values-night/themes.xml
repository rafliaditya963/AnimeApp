<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AnimeApp" parent="Theme.Material3.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/brown</item>
        <item name="titleTextColor">@color/orange_light</item>
        <item name="colorOnSecondaryContainer">@color/orange_light</item>
    </style>

    <style name="Theme.AnimeApp" parent="Base.Theme.AnimeApp" />
</resources>